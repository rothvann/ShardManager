syntax = 'proto3';

package psychopomp;

import "ServiceDiscovery.proto";

message ShardMapPartitions {
    repeated string sort_keys = 1;
    map<int64, Shard> shards = 2;
    map<int64, ShardMetrics> shard_metrics = 3;
}