syntax = 'proto3';

package psychopomp;

import "ServiceDiscovery.proto";

message ShardRangeMapPartitions {
    string partition_key = 1;
    repeated string sort_keys = 2;
}

message ShardRangeMap {
    repeated KeyspaceRange keyspace_ranges = 1;
}
